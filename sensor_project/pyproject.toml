[project]
name = "sensor-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "plotly>=7.0.0",
    "pyserial>=3.5",
    "requests>=2.34.2",
    "streamlit>=1.63.0",
]

[project.scripts]
sensor-project = "sensor_project:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
